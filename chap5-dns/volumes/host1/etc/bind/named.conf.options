options {
  directory "/var/cache/bind";
  //directory "/etc/bind";
  listen-on port 53 { 127.0.0.1; 192.168.1.12; };
  listen-on-v6 port 53 { ::1;};
  allow-query { any; }; // anyなら、どこからでもクエリを受け付ける。

  recursion no; // このDNSサーバは末端（サブドメインを持たない）であり、これ以上再帰問い合わせは不要なのでno。

  dnssec-validation auto;
}