本フォルダは「6.22 SSHによるリモートログイン」に対応しています。

# Dockerコンテナ起動後

以下の手順で進めると良いです。

- ssh-client(192.168.1.12)にexecで入る。
- ssh user1@192.168.1.11で、パスワード入力してSSHログインする。
- 一旦、exitで抜けた後、
- ssh user2@192.168.1.11で、公開鍵を用いてパスワード入力なしで、SSHログインする。